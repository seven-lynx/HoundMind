# Full dependencies for Pi4 (heavy preset)
# Includes vision, audio, and ML-friendly packages. Expect longer installs.

# Hardware bindings
pidog>=0.1.0

# Core numeric stack
numpy
scipy

# Vision
opencv-python
opencv-contrib-python
face_recognition

# Audio / speech
SpeechRecognition
pyaudio
sounddevice

# SLAM (RTAB-Map)
# Note: rtabmap-py may need to be built from source on Pi4. See scripts/install_rtabmap_pi4.md for instructions.
rtabmap-py

# Web utilities (used by installer/model downloader on Pi4)
flask

# Notes:
# - `face_recognition` depends on `dlib` which typically requires build tools
#   (cmake, build-essential) and additional libs. Install system packages
#   listed in docs/INSTALL.md before attempting to pip-install `face_recognition`.
