# Minimal desktop requirements for simulation (Windows/macOS/Linux)
# Avoids Pi-only and heavy packages (RPi.GPIO, adafruit-*, pidog, dlib, face-recognition, tensorflow).

# Core
importlib-metadata>=4.0.0
colorama>=0.4.6
pyyaml>=6.0
requests>=2.31.0
websockets>=11.0.0

# Optional voice (you can omit these two if not using voice)
SpeechRecognition>=3.10.0
pyaudio>=0.2.11
pydub>=0.25.1

# Vision (no dlib/face-recognition)
opencv-python>=4.8.0
pillow>=9.5.0

# Scientific stack
numpy>=1.24.0
scipy>=1.10.0
matplotlib>=3.7.0

# Dev tools (optional)
pytest>=7.4.0
black>=23.0.0
flake8>=6.0.0

# Telemetry (optional; install on demand)
# fastapi>=0.115.0
# uvicorn>=0.30.0
